# Flattened into the top-level PiP repo — the formerly-nested upstream clones
# (pi-web, pi-subagents, pi-mcp-adapter, pi-web-access) are now vendored here.

# Never vendor node_modules or real API keys
node_modules/
pi-qwen-rotate/qwen-rotate.config.json
